'use client';

import { ThemeToggle } from '@/components/app/theme-toggle';

/** Alternador de tema no header (reutiliza ThemeToggle do app). */
export function HeaderThemeSwitch() {
  return <ThemeToggle />;
}
